import styled from "styled-components";
import { motion } from "framer-motion";

export const LogopotLogoSvg = styled.svg`
  flex-shrink: 0;
  fill: currentColor;
`;

export const Bubble = styled(motion.path)`
  fill: currentColor;
`;
